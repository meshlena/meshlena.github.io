# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
*~

# Source archives — not part of the deployable site
*.zip

# Per-case working docs — only index.html + assets/ ship
cases/*/case*.md
cases/*/README.md
cases/*/artifacts/

# Local Claude Code settings (machine-specific)
.claude/
